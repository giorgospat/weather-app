package com.example.data.backend

object NetworkConstants {
    const val connectTimeoutSeconds: Long = 20
    const val readTimeoutSeconds: Long = 20
    const val writeTimeoutSeconds: Long = 20

    const val keyParam = "key"
}
